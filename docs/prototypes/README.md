# `docs/prototypes/` — disposable UX mocks

Static **HTML-only** prototypes for design review. **Not** served by `oceano-web` in production; safe to delete this folder when the real UI ships.

| File | Purpose |
|------|---------|
| `onboarding-hub-mock.html` | Config hub, checklist, amplifier wizard branches, calibration / now playing stubs — open locally (`file://` or any static server). |

See also: [plans/configuration-onboarding-improvement-plan.md](../plans/configuration-onboarding-improvement-plan.md).
